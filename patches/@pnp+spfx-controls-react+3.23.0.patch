diff --git a/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/DayView.js b/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/DayView.js
index 2596c1d..6f1d512 100644
--- a/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/DayView.js
+++ b/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/DayView.js
@@ -48,13 +48,19 @@ export var DayView = function (props) {
                 var weekDay = format(addDays(currentDay, dayIndex), 'yyyy-MM-dd');
                 var dayEvents = weekEvents.find(function (day) { return day.date === weekDay; });
                 var fullDayEvents = (dayEvents === null || dayEvents === void 0 ? void 0 : dayEvents.fullDayEvents) || [];
-                return (React.createElement("div", { key: dayIndex, className: styles.fullDayCell }, fullDayEvents.map(function (event) { return (React.createElement("div", { key: event.id, className: styles.fullDayEvent, style: {
+                return (React.createElement("div", { key: dayIndex, className: styles.fullDayCell }, fullDayEvents.map(function (event) {
+                    var customRender = typeof event.onRenderInDayView === 'function' ? event.onRenderInDayView(event) : null;
+                    if (customRender) {
+                        return React.createElement(React.Fragment, { key: event.id }, customRender);
+                    }
+                    return (React.createElement("div", { key: event.id, className: styles.fullDayEvent, style: {
                         gridColumn: "".concat(dayIndex + 2), // Position event in the correct day column
                         backgroundColor: event.color
                             ? getCalendarColors(event.color).backgroundColor
                             : getEventColors(event.category).backgroundColor,
                     } },
-                    React.createElement(Caption1Strong, { className: styles.eventTitle }, event.title))); })));
+                    React.createElement(Caption1Strong, { className: styles.eventTitle }, event.title)));
+                })));
             })));
     }, [
         currentDay,
diff --git a/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/Toolbar.js b/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/Toolbar.js
index 2561807..872f1f2 100644
--- a/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/Toolbar.js
+++ b/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/Toolbar.js
@@ -66,7 +66,7 @@ export var Toolbar = React.memo(function (_a) {
         onDayChange,
         setCurrentDate,
     ]);
-    return (React.createElement(Stack, { width: "calc(100% - 40px)", height: "fit-content", horizontal: true, horizontalAlign: "space-between", verticalAlign: "center", paddingLeft: "20px", paddingRight: "20px", paddingBottom: "10px", paddingTop: "10px", columnGap: 10, styles: { overflow: 'hidden' } },
+    return (React.createElement(Stack, { width: "100%", height: "fit-content", horizontal: true, horizontalAlign: "space-between", verticalAlign: "center", paddingLeft: "0px", paddingRight: "0px", paddingBottom: "10px", paddingTop: "10px", columnGap: 10, styles: { overflow: 'hidden' } },
         React.createElement(Stack, { horizontal: true, horizontalAlign: "start", verticalAlign: "center", columnGap: 10 },
             React.createElement(RenderToday, null),
             React.createElement(RenderSelectView, null),
diff --git a/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/WeekView.js b/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/WeekView.js
index c3f5501..fecbbe5 100644
--- a/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/WeekView.js
+++ b/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/WeekView.js
@@ -64,13 +64,19 @@ export var WeekView = function (props) {
                 var weekDay = formatDate(addDays(weekStart, dayIndex).toISOString(), 'yyyy-MM-dd');
                 var dayEvents = weekEvents.find(function (day) { return day.date === weekDay; });
                 var fullDayEvents = (dayEvents === null || dayEvents === void 0 ? void 0 : dayEvents.fullDayEvents) || [];
-                return (React.createElement("div", { key: dayIndex, className: styles.fullDayCell }, fullDayEvents.map(function (event) { return (React.createElement("div", { key: event.id, className: styles.fullDayEvent, style: {
+                return (React.createElement("div", { key: dayIndex, className: styles.fullDayCell }, fullDayEvents.map(function (event) {
+                    var customRender = typeof event.onRenderInWeekView === 'function' ? event.onRenderInWeekView(event) : null;
+                    if (customRender) {
+                        return React.createElement(React.Fragment, { key: event.id }, customRender);
+                    }
+                    return (React.createElement("div", { key: event.id, className: styles.fullDayEvent, style: {
                         gridColumn: "".concat(dayIndex + 2), // Position event in the correct day column
                         backgroundColor: event.color
                             ? getCalendarColors(event.color).backgroundColor
                             : getEventColors(event.category).backgroundColor,
                     } },
-                    React.createElement(Caption1Strong, { className: styles.eventTitle }, event.title))); })));
+                    React.createElement(Caption1Strong, { className: styles.eventTitle }, event.title)));
+                })));
             })));
     }, [
         weekStart,
@@ -83,8 +89,8 @@ export var WeekView = function (props) {
     var renderDayCells = React.useCallback(function () {
         var weekEvents = getWeekEvents(events, weekStart.toISOString());
         return Array.from({ length: 7 }, function (_, dayIndex) {
-            var weekDay = format(addDays(weekStart, dayIndex), 'yyyy-MM-dd');
-            var dayEvents = weekEvents.find(function (day) { return day.date === weekDay; });
+            // Use index-based mapping to avoid UTC/local date-key drift that can hide Saturday cells.
+            var dayEvents = weekEvents[dayIndex];
             return (React.createElement(React.Fragment, null, dayEvents === null || dayEvents === void 0 ? void 0 : dayEvents.timeSlots.map(function (timeSlot, slotIndex) {
                 var eventCount = timeSlot.events.length; // Total events in the slot
                 return (React.createElement("div", { key: slotIndex, className: styles.dayCell, style: {
diff --git a/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useCalendar.js b/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useCalendar.js
index 1c60c0b..36e442e 100644
--- a/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useCalendar.js
+++ b/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useCalendar.js
@@ -18,7 +18,13 @@ export var useCalendar = function (timezone) {
     }, []);
     // Memoized helper for timezone handling
     var toLocalDate = useCallback(function (dateString) {
-        return new Date(new Date(dateString).toLocaleString(undefined, { timeZone: timezone }));
+        var parsed = new Date(dateString);
+        if (isNaN(parsed.getTime())) {
+            return new Date();
+        }
+        // Force an en-US parseable format to avoid locale-dependent Invalid Date
+        // values (for example nl-NL day-month ordering) on the second Date parse.
+        return new Date(parsed.toLocaleString('en-US', { timeZone: timezone }));
     }, [timezone]);
     // Memoize getMonthCalendar to avoid re-creation unless dependencies change
     var getMonthCalendar = useCallback(function (events, year, month) {
diff --git a/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useCalendarStyles.js b/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useCalendarStyles.js
index b43f039..9bb0748 100644
--- a/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useCalendarStyles.js
+++ b/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useCalendarStyles.js
@@ -3,7 +3,6 @@ import { tokens } from "@fluentui/react-components";
 export var useCalendarStyles = function () {
     var styles = {
         calendarWrapper: css({
-            padding: "20px",
             paddingTop: "10px",
             overflow: "auto",
             flex: 1,
@@ -12,14 +11,14 @@ export var useCalendarStyles = function () {
             gridTemplateRows: "auto", // Automatically sizes the header row
             gridAutoRows: "minmax(100px, 1fr)", // Ensures all rows have a minimum height of 100px and grow equally
             gap: "1px", // Gap between cells
-            width: "calc(100% - 40px)", // Full width minus padding
+            width: "100%", // Full width
         }),
         header: css({
             display: "flex",
             justifyContent: "start",
             gap: "10px",
             alignItems: "center",
-            padding: "16px",
+            paddingTop: "16px",
         }),
         dayHeader: css({
             textAlign: "start",
diff --git a/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useDayViewStyles.js b/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useDayViewStyles.js
index 3fdcb8e..d327fa4 100644
--- a/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useDayViewStyles.js
+++ b/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useDayViewStyles.js
@@ -6,10 +6,10 @@ export var useDayViewStyles = function () {
         container: css({
             display: "flex",
             flexDirection: "column",
-            width: "calc(100% - 40px)",
+            width: "100%",
             height: "100vh", // Make the container take the full viewport height
             overflow: "hidden", // Prevent window scrolling
-            padding: "20px",
+            paddingTop: "10px",
         }),
         header: css({
             display: "flex",
@@ -88,6 +88,10 @@ export var useDayViewStyles = function () {
             padding: "5px",
             borderRight: "1px solid ".concat(tokens.colorNeutralStroke1), // Right border for the label cell
             borderBottom: "3px solid ".concat(tokens.colorNeutralStroke2), // Bottom border for the cell
+            position: 'sticky',
+            top: '50px', // Keep all-day row pinned below the sticky header row
+            zIndex: 9,
+            backgroundColor: tokens.colorNeutralBackground2,
         }),
         fullDayCell: css({
             borderRight: "1px solid ".concat(tokens.colorNeutralStroke1), // Right border between day cells
@@ -97,6 +101,10 @@ export var useDayViewStyles = function () {
             padding: "4px", // Ensure events have spacing within the cell
             borderBottom: "3px solid ".concat(tokens.colorNeutralStroke2), // Bottom border for the cell
             overflow: "hidden", // Ensure events don’t overflow the cell
+            position: 'sticky',
+            top: '50px', // Keep all-day row pinned below the sticky header row
+            zIndex: 9,
+            backgroundColor: tokens.colorNeutralBackground2,
         }),
         fullDayEvent: css({
             flex: "1 1 auto", // Allow events to share space equally
diff --git a/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useUtils.js b/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useUtils.js
index 832eb37..3b21f59 100644
--- a/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useUtils.js
+++ b/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useUtils.js
@@ -8,10 +8,10 @@ export var useUtils = function () {
         // Calculate the start and end slots based on event times
         var startHour = startDate.getHours();
         var startMinute = startDate.getMinutes();
-        var startSlot = startHour * 2 + (startMinute >= 1 ? 1 : 0); // Adjust for 30-minute slots
+        var startSlot = startHour * 2 + (startMinute >= 30 ? 1 : 0); // Adjust for 30-minute slots
         var endHour = endDate.getHours();
         var endMinute = endDate.getMinutes();
-        var endSlot = endHour * 2 + (endMinute >= 1 ? 2 : 0); // Adjust for 30-minute slots
+        var endSlot = endHour * 2 + (endMinute > 30 ? 2 : endMinute > 0 ? 1 : 0); // Adjust for 30-minute slots
         var spanSlots = endSlot - startSlot;
         return { spanSlots: spanSlots, startSlot: startSlot, endSlot: endSlot };
     }, []);
diff --git a/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useWeekViewStyles.js b/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useWeekViewStyles.js
index ba0d802..ef10fdd 100644
--- a/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useWeekViewStyles.js
+++ b/node_modules/@pnp/spfx-controls-react/lib/controls/calendar/hooks/useWeekViewStyles.js
@@ -6,10 +6,10 @@ export var useWeekViewStyles = function () {
         container: css({
             display: 'flex',
             flexDirection: 'column',
-            width: 'calc(100% - 40px)',
+            width: '100%',
             height: '100vh', // Make the container take the full viewport height
             overflow: 'hidden', // Prevent window scrolling
-            padding: '20px',
+            paddingTop: '10px',
         }),
         header: css({
             display: 'flex',
@@ -86,6 +86,10 @@ export var useWeekViewStyles = function () {
             padding: '5px',
             borderRight: "1px solid ".concat(tokens.colorNeutralStroke1), // Right border for the label cell
             borderBottom: "3px solid ".concat(tokens.colorNeutralStroke2), // Bottom border for the cell
+            position: 'sticky',
+            top: '50px', // Keep all-day row pinned below the sticky header row
+            zIndex: 9,
+            backgroundColor: tokens.colorNeutralBackground2,
         }),
         fullDayCell: css({
             borderRight: "1px solid ".concat(tokens.colorNeutralStroke1), // Right border between day cells
@@ -95,6 +99,10 @@ export var useWeekViewStyles = function () {
             padding: '4px', // Ensure events have spacing within the cell
             borderBottom: "3px solid ".concat(tokens.colorNeutralStroke2), // Bottom border for the cell
             overflow: 'hidden', // Ensure events don’t overflow the cell
+            position: 'sticky',
+            top: '50px', // Keep all-day row pinned below the sticky header row
+            zIndex: 9,
+            backgroundColor: tokens.colorNeutralBackground2,
         }),
         fullDayEvent: css({
             flex: '1 1 auto', // Allow events to share space equally
