# Copilot instructions

Read and follow [the repository agent instructions](../AGENTS.md) before you inspect or change the codebase.

Use `npm run build` instead of Gulp, but only when you debug the solution.

Use the documentation route in `AGENTS.md`. Do not duplicate architecture rules in this file.
